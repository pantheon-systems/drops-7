Image Style Overrides
=====================

This module is needed because default image styles CAN'T be overridden in
Features.  If they are a bunch of Express features will be overridden,  which
is bad.
